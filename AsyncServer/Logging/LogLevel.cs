namespace Asterion.Logging {
    /**
     * Enum of log levels for the writeOutput method.
     */
    public enum LogLevel {
        Debug,
        Info,
        Warn,
        Error,
        Fatal
    }
}
